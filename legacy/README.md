# Legacy Files

- `supplied_files/` contains untouched copies of the files supplied for this
  cleanup.
- `github_before_cleanup/` contains the MATLAB files that were already present
  in the public repository before restructuring.

Use the cleaned files in `../code/` for normal execution.
